# Backlog — not delivered to reviewers

Retired / paused work kept for reference but OUT OF SCOPE for the paper submission and reviewer reproduction (like `archive/`). Track B (pooled/dependent-branch architecture) is paused; the delivered model is the parallel LSTM+GAT+news architecture.
