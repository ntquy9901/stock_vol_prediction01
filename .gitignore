# Claude Code
.claude/

# Agent tooling / skills (vendored, not project code)
.agents/

# BMad workflow
_bmad/
_bmad-output/

# Data files - allowing training data (will commit small files)
# Excluding only large data directories
archive/data_raw/all_available/
archive/data_raw/vn100/
archive/data_raw/vn100_enhanced/
archive/data_raw/hnx/
archive/data_raw/hnx_enhanced/
data/processed_all/
archive/data/processed_all/
# Intermediate sentiment/news datasets (regenerable; exclude from VCS)
data/raw/news/
data/sentiment_*/
archive/data/sentiment_*/

# External resources (copyrighted material) - reference PDFs only, not the draft itself
docs/paper/*.pdf
docs/paper/project_context.md
docs/paper/project_context*.md

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Test / coverage artifacts
.coverage
coverage*.xml
*.cover
htmlcov/
.tox/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
ENV/
.venv_gpu_encode/
.venv_vnstock/

# Skill install artifacts + scratch/temp dirs (not source)
.superpowers/
_tmp_compare/
temp/
tmp/

# News embedding cache (regenerable from crawl_data + PhoBERT, tens of GB, not source)
data/external_news_embeddings/

# Ad-hoc rollback snapshots of regenerable feature panels (not source, not the current result)
data/features/*_backup_*.parquet

# Human-readable CSV debug dumps of parquet feature panels (regenerable, large, not source)
data/features/*_DEBUG.csv
data/features/DEBUG_by_ticker/

# Jupyter Notebook
.ipynb_checkpoints
*.ipynb
# ...except the Colab training notebook (must be on git so Colab can `git clone` it)
!notebooks/train_volga_colab_a100.ipynb

# PyTorch
*.pth
*.pt

# MLflow
mlruns/

# Experiments
experiments/
*.pkl
*.joblib

# IDEs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Backup files
*.bak
*.bak_*

# Temp files
temp_github_dataset

# Local Git worktrees
.worktrees/
archive/data_raw_prices_backup_pre_update_2026-08-16/

# seq-lookback experiment transient run logs (regenerable, not source)
scripts/seq_lookback/logs15/
scripts/seq_lookback/_run*.log
scripts/seq_lookback/_last_*_ts.txt

scripts/seq_lookback/_run_ablate.log
scripts/seq_lookback/_last_ablate_ts.txt

# model checkpoints in results are large + regenerable; keep metrics/dumps (json), not weights
results/**/*.pt

# S&P500 raw = Yahoo Finance (redistribution restricted by ToS); local only, not committed
data/raw/prices/sp500/

# S&P500 processed = derived from Yahoo (redistribution restricted); local only
data/processed/sp500/

scripts/sp500_crossmarket/_run.log

# SOICT experiment run outputs (regenerable)
results/soict/

submission/soict_lstm_gat/data/sp500/
submission/soict_lstm_gat/logs/

results/soict_perobs/

# Enriched processed data (baseline 2026-08-31_enriched_processed): commit only vn30 + vn100 (the tracked
# source markets); hose/hnx/sp500 enriched are local-only (large; sp500 redistribution-restricted like
# data/processed/sp500).
data/processed_enriched/hose/
data/processed_enriched/hnx/
data/processed_enriched/sp500/
# archived 2026-09-05: superseded by sp500_clean (pre-vintage-screen enriched, 544 tickers), kept local-only
archive/data_processed_enriched/sp500/

# reference paper PDFs (open-access; kept local, indexed by docs/papers/README.md)
docs/papers/*.pdf
