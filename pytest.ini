[pytest]
markers =
    smoke: smoke test — boots a pipeline/app and runs one happy-path. Must pass before a change is "done" (CLAUDE.md Project Quality Rules).
testpaths =
    src
    baselines
addopts = -ra --import-mode=importlib
filterwarnings =
    ignore::DeprecationWarning
